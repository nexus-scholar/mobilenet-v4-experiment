experiment_name: "mobilenetv4_tomato_domain_shift"

data:
  csv_path: "data/unified_dataset.csv"
  batch_size: 32
  num_workers: 4
  pin_memory: true

model:
  num_classes: 89
  use_segmentation: false

training:
  epochs: 25
  learning_rate: 0.0001
  alpha: 0.0
  beta: 0.0
  ignore_index: -1

device: "cuda"
